# Build artifacts
/_build/
/deps/
/cover/
/doc/
erl_crash.dump
*.ez
*.beam
/.elixir_ls/

# Env
.env
*.secret

# OS
.DS_Store

# Python bytecode from clients/
__pycache__/
*.pyc

# ---------------------------------------------------------------------------
# Intern solutions — kept locally, never committed. My reference copies of the
# exercises live under solutions_local/ (clients per transport, the OTLP
# bridges, and the Grafana dashboards). Interns implement their own versions in
# clients/ and deploy/grafana/dashboards/, which ARE tracked.
# ---------------------------------------------------------------------------
solutions_*/
